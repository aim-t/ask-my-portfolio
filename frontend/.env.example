# Copy this file to .env and point it at your running backend.
# Used by both the standalone app build and the widget build - the
# widget bakes this value in at build time, the same way you'd edit
# API_BASE_URL in widget/chat-widget.html before deploying.

VITE_API_BASE_URL=http://localhost:8000
